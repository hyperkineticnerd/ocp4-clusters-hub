kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: bitwarden-via-cf-tunnel
  namespace: bitwarden
  # annotations:
  #   route.openshift.io/termination: edge
spec:
  ingressClassName: cloudflare-tunnel
  rules:
    - host: bitwarden.hknlab.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: webui
                port:
                  name: http
