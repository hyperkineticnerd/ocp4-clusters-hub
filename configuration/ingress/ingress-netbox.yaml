kind: Ingress
apiVersion: networking.k8s.io/v1
metadata:
  name: netbox-via-cf-tunnel
  namespace: netbox
  # annotations:
  #   route.openshift.io/termination: edge
spec:
  ingressClassName: cloudflare-tunnel
  rules:
    - host: netbox.hknlab.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: webui
                port:
                  name: http
